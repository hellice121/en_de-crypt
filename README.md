en_de-crypt is a lightweight Python utility to encrypt and decrypt text files using a base algorithm. It allows you to take a plaintext file and generate an encrypted version, and likewise decrypt an encrypted file back into readable text.

To use it:
make a file named "based.txt"
and type the text/message to be encrypted 
and run the program .
